# This fixture is hashed as embedded release authority. A Windows checkout must
# not rewrite its bytes before `include_str!` embeds it in the executable.
crates/sister-assets/tests/fixtures/public-manifest-selected-v2.json text eol=lf
