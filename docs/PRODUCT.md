# AI-Sister — Final Product Definition

> 一個站在桌面角落的字母人。她一直都在，看得見你的一天，記得住細節，
> 95% 的時間安靜，該說話的時候才說話——而且說的每一句都能點開證據。

本文件是 2026-08-17 四模型 roundtable（Claude / Gemini / Grok / ChatGPT × 7 題 × 5 輪）
辯論收斂後的最終產品定義。技術規格見 [SPEC.md](SPEC.md)，開發階段見 [PHASES.md](PHASES.md)。

---

## 1. 一句話定義

**AI-Sister 是一個 local-first 的桌面陪伴體：一個「絕對不會忘記的本機檔案櫃」+
一個「事件驅動、會認錯的理解引擎」+ 一個「知道什麼時候閉嘴的字母人」。**

表面上是一個無害的桌面角色；背後是持續落地的原始證據層、偶發喚醒的意圖理解層、
可結案的記憶層。使用者可以隨時問她兩小時前的事，她秒答並附出處；
她偶爾（一天幾次以內）主動開口，講的是真正值得講的事。

## 2. 這個產品「不是」什麼

辯論中最重要的定位修正（第 7 題）：

- **不是「手」**——UI-TARS / Agent S / UFO / OmniParser 解的是「看到畫面、吐下一個動作」。
  那些是可以整合的元件，不是我們的賽道。
- **是「記性」**——真正的同賽道對手是 Microsoft Recall 與 Rewind/Limitless
  （後者已被 Meta 收購、2025-12-19 停止螢幕/聲音錄製）。
- 而這條賽道的屍體告訴我們：**死因從來不是準確率**。Rewind 的死法是
  「使用者裝了、用三次、然後忘記自己裝過」。Recall 的困境是信任。
  所以本產品的兩個真正戰場是：**信任（敢讓她一直開著）** 與 **習慣（三個月後還記得問她）**。

## 3. 核心信條（Product Principles）

這八條是整場辯論的結晶，設計與取捨一律回到這裡：

1. **暴力用在保存，不用在生成。** 存資料幾乎免費，生敘事又貴又會歪。
   原始證據無腦落地、不經 LLM、不總結、不刪改。
2. **抄寫歸程式，意圖歸模型。** 金額、電話、URL、error code、檔名、剪貼簿——
   regex + OCR 在本機抓，零成本、零幻覺。模型只回答「他在幹嘛、為什麼」。
3. **證據不可變，解釋可推翻，記憶可結案。** 三層真相模型：
   下層 append-only 不准動；中層是掛著信心值與出處的假設；上層是可以死的記憶。
4. **開口那一秒是唯一的考試。** 使用者永遠不看後台。一天講五次中四次是助理；
   一天講三十次中六次，準確率一樣，第三天就會被關掉。沉默是預設值。
5. **敢開著，比聰明重要。** 原始畫面一粒 pixel 都不上雲。上雲的只有去識別化後的
   少量文字，而且是三張分開的同意書。這是 open source 對上 Copilot 唯一的生路。
6. **每句話查得到出處。** 她說的任何事實都能點開當時的畫面。
   說不確定的時候用「我最後看到的是……」，不說「你還沒做」。
7. **犯錯要有邊界。** 不做永遠判斷正確的字母人（做不到），
   做一個就算判斷錯、也只能在明確範圍內犯錯的字母人。
8. **沒有「缺一不可」。** 每個元件都可以被單獨關掉、量測、砍掉。
   真正缺一不可的只有三樣：出處可查、記憶（連同衍生內容）刪得掉、知道何時閉嘴。

## 4. Killer Scenarios（按交付順序）

### S1 — 秒答回憶（Sister 1.0 的全部）
> 「你還記得我之前打的那個帳單大概多少錢嗎？那個客服電話幾號？」
> 「我昨天下午在弄什麼？卡在哪？」

本機全文檢索 + 事實層直查，retrieval < 100ms，附出處畫面。
LLM 只在「把十幾筆結果講成一句人話」時被呼叫一次（可完全離線降級成結果列表）。
**這是零 agent 就能做到的功能，也是整個產品的地基與第一週魔法時刻的來源。**

### S2 — 帶著上下文接手（真正的差異化）
> 「我最後看到你卡在 Cloudflare DNS，後來沒有看到完成證據。
> 如果還沒處理，我可以替你打開那個頁面——送出前我會停下來。」

一句話同時展現：記憶、證據邊界（承認自己可能漏看）、行動能力、權限範圍。
行事曆和待辦軟體給不了這個；這是和一般桌面寵物拉開差距的地方。

### S3 — 溫柔且稀有的主動開口
> 「剛剛會計師好像 LINE 你，你好像沒注意到。」
> 「你女朋友叫你五點去接她，我覺得你快來不及了。」
> 「milestone 3 好像還有兩件事沒處理，要我現在幫你做一下嗎？」

有每日開口預算、有分數門檻、有 quiet hours。每句話帶「結案」按鈕——
使用者說「我弄好了」，那條記憶當場死掉。

### S4 — 筆記與交接
> 「要不要我幫你把今天做的事情做個筆記？」
> 「你要出門了嗎？要不要我接著幫你把這個專案推進下去——
> 我會遵照你們討論出來的 spec，程式留在 staging，不碰 production。」

日終摘要、工作交接、以及（後期）在明確邊界內接手操作——包含監督 CLI coding agents
這種 Ted 自己每天在做的事。

## 5. 目標使用者

**第一圈：開發者 / power users。** 他們容忍 alpha、有 API key 或 AI 訂閱、
最能感受 recall 的價值，而且他們就是 GitHub 的觀眾。
**第二圈**（announcement 之後）：知識工作者。

## 6. 競品與差異化

| 產品 | 現況（2026-08 查證） | 教訓 / 我們的差異 |
|---|---|---|
| Microsoft Recall | opt-in、<10% Win11 機型合格、2026-04 仍被 TotalRecall Reloaded 打穿、至今無 master 開關 | 「宣稱本機」不夠——要**可驗證**的加密、審計與 kill switch |
| Rewind / Limitless | 2025-12-05 被 Meta 收購、12-19 強制停錄、domain 已被搶註 | **closed product 退場 = 你的記憶滅絕** → 開源 + 開放資料格式是結構性信任優勢 |
| Screenpipe | 2026-06-10 從 MIT 改自訂商業授權（禁商用/禁嵌入/禁競品） | 讓出「本類別唯一真開源」的位子；只當架構參考（它自承 10% CPU / 4GB RAM / 15GB/月——我們的資源預算是它 1/3 以下） |
| Everywhere | v0.8.x 活躍、BUSL 1.1、快捷鍵召喚型、Strategy Engine 主動性仍未出貨 | 不同賽道（當下 vs 整天）；MCP interop 可以，**程式碼不可用**（見下方〔定案〕） |
| OpenAI Pulse | 2026-08 被砍 | **主動性是墳場**——雲端淺脈絡的主動 = 垃圾訊息；我們的主動是本機證據門檻 + 每日預算，另一種動物 |
| Littlebird / Highlight / Cluely | $11M / $40M A 輪 / 信譽受損但活著 | 全部 cloud-first 閉源——把「local-first + 可驗證」讓給了我們 |
| 各家桌面 Copilot（ChatGPT 桌面、Claude Cowork、Gemini on Windows） | 全部是 on-demand 看螢幕 | 巨頭拿走了「當下」，沒人做「24/7 本機記憶 + 稀有主動」 |

**「讀過再自己寫」的界線**〔定案〕：可以，但理由不是「產品不一樣」。

BUSL 1.1 管的是**對 Licensed Work（他們的程式碼）的使用**，不管「讀」，也不管
「與它對話」。所以讀原始碼學招合法、MCP/API interop 合法——這一點
`research/landscape.md` §4 已逐字查證過 LICENSE 原文。

但「畢竟是不一樣的產品」這句話**對我們特別不成立**，剛好是反過來的：Sylinko 的
Additional Use Grant 把 Competing Use 定義成「作為替代品在商業上提供、以商業
形式提供功能近似的服務」，而 ambient 螢幕助理正是我們在做的事。別人可能落在
Permitted Purpose 裡，我們沒有那個緩衝，一路到 2030 年 Change Date 轉 Apache 2.0
之前都沒有。所以我們要守的線比一般專案更前面，不是更後面。

而且真正的風險不是被告，是一個 issue 寫著「這是抄 Everywhere 的」。這個 repo
的目的是累積名聲，那種指控就算不成立也已經達到效果了。

操作規則（照這個做就不會有事）：
1. **要什麼**：只拿「哪些 Windows 資源值得擷取、代價多大」——那是關於
   Windows 的**事實**，事實不受著作權保護，本來就抄不到。
2. **怎麼寫**：看過之後把結論寫成筆記，**關掉他們的原始碼再動手**。從筆記寫，
   不要從 buffer 寫。「我重打過一遍」在法律上不是抗辯。
3. **不准帶走**：命名、常數、模組切法、函式分解順序、註解、測試資料。這些是
   expression，不是 idea——結構一模一樣的重寫仍然是 derivative work。
4. **記出處**：訊號來源在 DATA_INVENTORY 註明是從哪學來的。有標註的相似叫
   致敬，沒標註的相似叫抄襲，差別只在有沒有寫下來。
5. 純粹是 OS 的東西（UIA property ID、COM 介面名、event 常數）引 Microsoft
   文件，不要引他們——那本來就不是他們的。

**Gap 陳述（2026-08）**：市場上沒有任何玩家同時具備
真開源授權 × local-first × 硬隱私 × 24/7 螢幕記憶 × 便宜的主動觸發 × 陪伴角色。
「手」已商品化（Agent S3 在 OSWorld 72.6% 超過人類基線；UFO²/OmniParser v3 皆 MIT）
——**capture 與 hands 用組的，記憶 schema、守門員、角色、可驗證隱私才是自建核心。**

**我們的護城河不是資料**（樣本數 = 1，那只是調參依據），而是：

1. **重播評測工具鏈 + 公開數字**——「市面上沒人敢公布這種數字」。
   錄真實工作流、重播、比架構、公布找回率/誤提醒率/成本。資料是你的，工具是大家的。
2. **隱私架構本身**——README 第一行就把三張同意書講得比微軟硬，
   而且可驗證（§SPEC 11.7）、可帶走（§SPEC 11.8）。
3. **品味**——「什麼時候閉嘴」的守門員是沒有論文可抄的部分，靠評測語料磨出來。

**核心演算法賽道**（沒人解過、也沒題庫）：**斷句**。
既有產品白拿使用者按 Enter 的任務邊界；我們面對的是從開機到關機一條沒有標點的流。
如何把一整天切成「日後問得到、答得快、查得回來源」的段落，就是本專案的技術核心。

## 7. Non-Goals（v1 明確不做）

- ❌ 雲端服務 / 帳號系統（local-first；cloud sync 遠期選配、E2EE）
- ❌ 音訊 / 麥克風錄製（Limitless 的死線 + 旁人同意問題最尖銳的地方）
- ❌ Autopilot（記憶還可能記歪之前，不讓它碰滑鼠；見 PHASES Phase 7 的門檻）
- ❌ 一直講話的桌寵（我們是為了讓她安靜而設計整個引擎的）
- ❌ 六套框架 adapter（單人維護的最常見死因；v1 只用通用 OS API，
  per-app adapter 走後期 plugin 生態）
- ❌ 手機版

## 8. 開源與授權立場

- **Repo 公開 ≠ 正式發布。** Phase 1 完成即開 repo（標 alpha、預設只看不講話），
  正式宣傳（Show HN / X）等到 Phase 5 帶著 benchmark 數字一起。
- **授權：Apache-2.0。** 目標是名聲與採用，不是營收；local-first 產品不怕被託管
  （Everywhere 選 BUSL 防的那件事對我們不成立）。若遠期出現 cloud sync 服務，
  再單獨處理那個 repo 的授權。
- **維護策略：** v1 訊號面收斂到通用 API（capture / OCR / window / clipboard），
  把「Photoshop 抓不到字」這類 per-app 地獄隔離在 plugin 層，社群共養。

## 9. 命名與視覺識別

- 專案名：**AI-Sister**。這不是新造的名字——TokenMonster 的視窗標題本來就是
  "Token Monster (AI-Sister)"，角色資產的 R2 bucket 名就叫 `ai-sister`。
  本專案是這條血脈的正主。
- 桌面角色：**字母人（letter-avatar）為 day-one 識別**——直接繼承 TokenMonster 的
  `letter-wardrobe` 系統（code-native、零資產依賴、離線可用、WCAG 對比保證）。
  狀態只用形態與微光表達，不彈窗、不搶焦點。
- **姊妹 persona catalog 直接繼承**：Aster / Cedar / Mira / Rook 四姊妹
  （alias + glyph + zh-TW tagline + personaContext/safeguards 的型式照搬），
  完整立繪（891 圖 + 55 語音，`cdn.ted-h.com` R2 pipeline）在使用者明確同意後
  單次下載疊加。使用者選一位姊妹當常駐陪伴體。
- 互動 DNA：tap-lines 決定性台詞引擎（168 條 zh-TW 語料現成）、
  portrait crossfade state machine、`prefers-reduced-motion` 慣例——全部沿用。

## 10. 成功指標（產品層）

| 指標 | 定義 | 目標 |
|---|---|---|
| 第一週魔法時刻 | 安裝 7 天內答對一次「使用者自己都忘掉的東西」 | 自用驗證 → 每位新用戶 |
| 第二次開口率 | 第一週問過一次的人，會不會再問 | 這是 Rewind 死掉的那一題 |
| 誤提醒率 | 主動開口被按掉/否定的比例 | < 40%（開口預算 ≤ 5/天） |
| 漏提醒率 | 事後標注「當時該講而沒講」 | 用 replay 語料量測 |
| 三個月留存 | 90 天後仍每天常駐 | 產品的真正考試 |
| 資源足跡 | CPU / RAM / 電池 / 磁碟 | 見 SPEC §13 預算 |
| 月成本 | 預設檔位的 API 花費 | 見 SPEC §12 成本模型 |
