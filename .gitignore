node_modules/
target/
dist/
# Tauri 的 build script 每次自己重生這份權限 schema
apps/desktop/src-tauri/gen/
.DS_Store
*.log
.env

# 在本機重跑 ci.yml 那一大段（「An erased database must not look like one that
# never recorded」）造出來的 fixture 和輸出。那支腳本跑完會自己清掉——但**中途
# 紅掉就會留在原地**，而下一次 `git add -A` 會把一顆測試資料庫掃進一個 public
# repo。清理只走成功那條路，這幾行顧的是失敗那條。
/ci-*
/boot-*.txt
/live-*.txt
/null-*.txt
/first-*.txt
/first-stats.json
/crash-*.txt
/crashed-*.txt
/erased-*.txt
/normal-*.txt
/upgrade*.txt*
/stop-*.txt
/booting*.txt
/livecrash.txt
/forget.txt
/d.txt
/f.txt
/s.txt

# 私下 roundtable 辯論的逐字萃取 — 設計源頭，不公開
research/extracts/
# 自有 repo 的內部盤點（含資產授權待確認事項）— 不公開
research/ted-repos-dna.md
/target

# 交接給 codex 的暫存：上一輪跑完但沒 commit 的 patch + 接手計畫
.handoff/

# 真實工作日匯出的 replay 私有草稿。即使指令在 repo 裡執行、途中失敗，
# `git add -A` 也不可以把同事／客戶的 OCR 文字送進 public repo。
*.sister-replay-draft.json
*.sister-eval-report.json
replay-drafts/
