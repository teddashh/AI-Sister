node_modules/
target/
dist/
# Tauri 的 build script 每次自己重生這份權限 schema
apps/desktop/src-tauri/gen/
.DS_Store
*.log
.env

# 在本機重跑 ci.yml 那一大段（「An erased database must not look like one that
# never recorded」）造出來的 fixture 和輸出。那支腳本跑完會自己清掉——但**中途
# 紅掉就會留在原地**，而下一次 `git add -A` 會把一顆測試資料庫掃進一個 public
# repo。清理只走成功那條路，這幾行顧的是失敗那條。
/ci-*
/boot-*.txt
/live-*.txt
/null-*.txt
/first-*.txt
/first-stats.json
/crash-*.txt
/crashed-*.txt
/erased-*.txt
/normal-*.txt
/upgrade*.txt*
/stop-*.txt
/booting*.txt
/livecrash.txt
/forget.txt
/d.txt
/f.txt
/s.txt

# 私下 roundtable 辯論的逐字萃取 — 設計源頭，不公開
research/extracts/
# 自有 repo 的內部盤點（含資產授權待確認事項）— 不公開
research/ted-repos-dna.md
/target

# 交接給 codex 的暫存：上一輪跑完但沒 commit 的 patch + 接手計畫
.handoff/

# 真實工作日匯出的 replay 私有草稿。即使指令在 repo 裡執行、途中失敗，
# `git add -A` 也不可以把同事／客戶的 OCR 文字送進 public repo。
*.sister-replay-draft.json
*.sister-questions-draft.json
*.sister-eval-report.json
replay-drafts/

# Delegate 的交付報告，以及派給它的那張工作單。兩者都寫在工作區根目錄，而
# 收尾的 `git add -A` 會把它們一起收進來（alpha.62 的 RESULT 真的發生過一次，
# 事後才從 main 上拿掉；r28b 的 `TASK-r28b.md` 又發生一次）。
#
# 工作單那半是 `check-docs-point-somewhere.py` 抓到的。這裡原本寫「那些行號在
# delegate 動手的那一刻就漂掉了」——**那是假的**，我後來逐一比對過，`main.rs:1800`
# 和 `brain.rs:1015` 改動前後都精準指著被改的那兩個呼叫（hunk 是 1 換 1）。
# 真正的成因和行號對不對無關：那支閘門把反引號裡的整串當成路徑去 `.exists()`，
# 而沒有任何檔案真的叫做 `main.rs:1800`，所以**只要寫了「檔:行」就一定紅**。
#
# ⚠ 而且底下這幾行**擋不住那道閘門**：`.gitignore` 只擋 `git add`，閘門掃的是
#   工作區。r28c 的 `TASK-r28c.md` 已經在忽略名單裡，閘門照樣紅（四條，全部指
#   著我自己寫的行號）。工作單和交付報告收工時要**移出工作區**，不是只靠忽略。
RESULT.md
RESULT*.md
TASK*.md
