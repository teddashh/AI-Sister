[package]
name = "sister-usage"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
description = "Observed usage versus remaining quota versus unknown, plus optional LimitReset public status GET"
publish = false

[features]
default = []
# Default graph is parse/policy only. Only desktop may enable the native HTTPS GET.
public-status = ["dep:ureq"]

[dependencies]
serde.workspace = true
serde_json.workspace = true
chrono.workspace = true
ureq = { workspace = true, optional = true }

[dev-dependencies]
serde_json.workspace = true
